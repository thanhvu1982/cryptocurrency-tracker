import React, { FC } from 'react';
import CurrencyCardList from '../components/common/CurrencyCardList';

const Home: FC = () => {
  return (
    <div>
      <CurrencyCardList />
    </div>
  );
};

export default Home;
